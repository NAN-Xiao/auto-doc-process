# auto-doc-process dependencies (full / development)
#
# 包含 PyTorch + sentence-transformers（开发环境）。
# 服务器轻量部署请使用 requirements-server.txt（用 ONNX 替代 torch）。

# Feishu API
requests>=2.31.0
lark-oapi>=1.5.0

# Document processing
PyMuPDF>=1.24.0
python-docx>=1.0.0
lxml>=5.0.0

# LangChain text splitting & LLM
langchain-text-splitters>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
langchain-huggingface>=1.0.0
openai>=1.0.0

# Embedding model（开发环境：torch + sentence-transformers）
sentence-transformers>=3.0.0
torch>=2.0.0

# ONNX Runtime（服务器和开发环境都需要，用于导出/推理）
onnxruntime>=1.17.0
tokenizers>=0.19.0

# PostgreSQL + pgvector
psycopg[binary]>=3.1.0
pgvector>=0.3.0

# LightRAG knowledge graph
lightrag-hku>=1.4.0

# Retry
tenacity>=8.2.0

# Config
PyYAML>=6.0
