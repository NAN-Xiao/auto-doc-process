# auto-doc-process 依赖
#
# 默认安装即为轻量模式（ONNX 推理）。
# 如需开发环境（PyTorch + HuggingFace），取消底部注释。

# ─── Feishu API ───
requests>=2.31.0
lark-oapi>=1.5.0

# ─── 文档处理 ───
PyMuPDF>=1.24.0
python-docx>=1.0.0
lxml>=5.0.0

# ─── 文本拆分 ───
langchain-text-splitters>=1.0.0
langchain-core>=1.0.0

# ─── LLM（图片智能命名） ───
openai>=1.0.0

# ─── Embedding（ONNX 推理） ───
onnxruntime>=1.17.0
tokenizers>=0.19.0
numpy>=1.24.0

# ─── PostgreSQL + pgvector ───
psycopg[binary]>=3.1.0
pgvector>=0.3.0

# ─── LightRAG 知识图谱 ───
lightrag-hku>=1.4.0

# ─── 工具 ───
tenacity>=8.2.0
PyYAML>=6.0

# ─── 开发环境可选（PyTorch + HuggingFace，取消注释即可） ───
# torch>=2.0.0
# sentence-transformers>=3.0.0
# langchain-huggingface>=1.0.0
# langchain-openai>=1.0.0
