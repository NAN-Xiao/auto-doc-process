# auto-doc-process 轻量服务器依赖
#
# 用 ONNX Runtime 替代 PyTorch（磁盘 ~0.5GB vs ~1.3GB）
# 需要先在开发环境中运行: python tools/export_onnx.py 导出 ONNX 模型
# 然后将 models/onnx/ 目录复制到服务器

# ─── Feishu API ───
requests>=2.31.0
lark-oapi>=1.5.0

# ─── 文档处理 ───
PyMuPDF>=1.24.0
python-docx>=1.0.0
lxml>=5.0.0

# ─── LangChain 核心（仅文本拆分） ───
langchain-text-splitters>=1.0.0
langchain-core>=1.0.0
# 注意: 不需要 langchain-openai（已替换为原生 openai SDK）
# 注意: 不需要 langchain-huggingface（ONNX 模式下不使用）

# ─── LLM 调用（图片智能命名） ───
openai>=1.0.0

# ─── Embedding（轻量 ONNX 推理） ───
onnxruntime>=1.17.0
tokenizers>=0.19.0
numpy>=1.24.0
# 注意: 不需要 torch, sentence-transformers, scipy, sympy

# ─── PostgreSQL + pgvector ───
psycopg[binary]>=3.1.0
pgvector>=0.3.0

# ─── LightRAG 知识图谱 ───
lightrag-hku>=1.4.0

# ─── 重试 ───
tenacity>=8.2.0

# ─── 配置 ───
PyYAML>=6.0

